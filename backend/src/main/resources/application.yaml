spring:
  application:
    name: 01blog
  datasource:
    url: jdbc:postgresql://0.0.0.0:5432/mainDB
    username: abdnour
    password: abdnour
  jpa:
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  admin:
    email: abdnour@blog.com
    username: 1stNakazaki
    password: no pain no gain
  jwt:
    secret: ${JWT_SECRET:dev-only-secret-please-change-me-to-32-bytes-minimum}
    expiration-ms: 3600000

